#Инициация нового git:
git init

#привязка к удаленному репозитоиу (следует казать правильный)
git remote add origin https://github.com/Varghoth/SilentListener.git

#Проверка статуса:
git status

#простой коммит:
git add . 
git commit -m "Рабочий докер с ГУИ, фоксом, включенным звуком и т.д. Можносмотреть видосы и слушать музыку"


# заливка на гитхаб
git push -u origin main