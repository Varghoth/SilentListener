#Existing profile deletion

browser.storage.local.remove("userProfile").then(() => {
    console.log("Existing profile deleted. A new profile will be generated on the next run.");
}).catch((error) => {
    console.error("Error deleting user profile:", error);
});

#Testing page
amiunique.org

#Debug Menu
about:debugging

#firefox cache page
about:preferences#privacy